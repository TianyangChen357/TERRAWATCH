# Copy to /etc/terrawatch/environment, make it readable only by root and the
# terrawatch service account, then add the real value.  Never commit that file.
EARTHDATA_TOKEN=replace-with-a-NASA-Earthdata-token
